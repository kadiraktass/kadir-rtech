

#include <ros/ros.h>
#include <visualization_msgs/Marker.h>


int main( int argc, char** argv )
{
  ros::init(argc, argv, "basic_shapes");
  ros::NodeHandle n;
  ros::Rate r(1);
  ros::Publisher marker_pub = n.advertise<visualization_msgs::Marker>("visualization_marker", 1);


  while (ros::ok())
  {
    visualization_msgs::Marker marker;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker.header.frame_id = "/my_frame";
    marker.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker.ns = "basic_shapes";
    marker.id = 0;
    marker.type = visualization_msgs::Marker::CYLINDER;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker2;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker2.header.frame_id = "/my_frame";
    marker2.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker2.ns = "basic_shapes";
    marker2.id = 1;
    marker2.type = visualization_msgs::Marker::SPHERE;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker2.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker3;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker3.header.frame_id = "/my_frame";
    marker3.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker3.ns = "basic_shapes";
    marker3.id = 2;
    marker3.type = visualization_msgs::Marker::CYLINDER;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker3.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker4;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker4.header.frame_id = "/my_frame";
    marker4.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker4.ns = "basic_shapes";
    marker4.id = 3;
    marker4.type = visualization_msgs::Marker::SPHERE;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker4.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker5;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker5.header.frame_id = "/my_frame";
    marker5.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker5.ns = "basic_shapes";
    marker5.id = 4;
    marker5.type = visualization_msgs::Marker::CYLINDER;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker5.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker6;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker6.header.frame_id = "/my_frame";
    marker6.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker6.ns = "basic_shapes";
    marker6.id = 5;
    marker6.type = visualization_msgs::Marker::SPHERE;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker6.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker7;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker7.header.frame_id = "/my_frame";
    marker7.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker7.ns = "basic_shapes";
    marker7.id = 6;
    marker7.type = visualization_msgs::Marker::CYLINDER;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker7.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker8;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker8.header.frame_id = "/my_frame";
    marker8.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker8.ns = "basic_shapes";
    marker8.id = 7;
    marker8.type = visualization_msgs::Marker::SPHERE;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker8.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker9;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker9.header.frame_id = "/my_frame";
    marker9.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker9.ns = "basic_shapes";
    marker9.id = 8;
    marker9.type = visualization_msgs::Marker::CYLINDER;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker9.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker10;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker10.header.frame_id = "/my_frame";
    marker10.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker10.ns = "basic_shapes";
    marker10.id = 9;
    marker10.type = visualization_msgs::Marker::SPHERE;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker10.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker11;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker11.header.frame_id = "/my_frame";
    marker11.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker11.ns = "basic_shapes";
    marker11.id = 10;
    marker11.type = visualization_msgs::Marker::SPHERE;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker11.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker12;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker12.header.frame_id = "/my_frame";
    marker12.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker12.ns = "basic_shapes";
    marker12.id = 11;
    marker12.type = visualization_msgs::Marker::CYLINDER;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker12.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker13;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker13.header.frame_id = "/my_frame";
    marker13.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker13.ns = "basic_shapes";
    marker13.id = 12;
    marker13.type = visualization_msgs::Marker::SPHERE;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker13.action = visualization_msgs::Marker::ADD;


    visualization_msgs::Marker marker14;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker14.header.frame_id = "/my_frame";
    marker14.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker14.ns = "basic_shapes";
    marker14.id = 13;
    marker14.type = visualization_msgs::Marker::CYLINDER;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker14.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker15;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker15.header.frame_id = "/my_frame";
    marker15.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker15.ns = "basic_shapes";
    marker15.id = 14;
    marker15.type = visualization_msgs::Marker::SPHERE;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker15.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker16;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker16.header.frame_id = "/my_frame";
    marker16.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker16.ns = "basic_shapes";
    marker16.id = 15;
    marker16.type = visualization_msgs::Marker::CYLINDER;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker16.action = visualization_msgs::Marker::ADD;

    visualization_msgs::Marker marker17;
    // Set the frame ID and timestamp.  See the TF tutorials for information on these.
    marker17.header.frame_id = "/my_frame";
    marker17.header.stamp = ros::Time::now();
    // Set the namespace and id for this marker.  This serves to create a unique ID
    // Any marker sent with the same namespace and id will overwrite the old one
    marker17.ns = "basic_shapes";
    marker17.id = 16;
    marker17.type = visualization_msgs::Marker::SPHERE;
    // Set the marker action.  Options are ADD, DELETE, and new in ROS Indigo: 3 (DELETEALL)
    marker17.action = visualization_msgs::Marker::ADD;

    // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker.pose.position.x = 0;
    marker.pose.position.y = 0;
    marker.pose.position.z = 0;
    marker.pose.orientation.x = 0.0;
    marker.pose.orientation.y = 0.0;
    marker.pose.orientation.z = 0.0;
    marker.pose.orientation.w = 0.0;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker.scale.x = 0.5;
    marker.scale.y = 0.5;
    marker.scale.z = 2.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker.color.r = 0.0f;
    marker.color.g = 0.0f;
    marker.color.b = 0.0f;
    marker.color.a = 1.0;
    marker.lifetime = ros::Duration();

        // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker2.pose.position.x = 0;
    marker2.pose.position.y = 0;
    marker2.pose.position.z = 0.75;
    marker2.pose.orientation.x = 0.0;
    marker2.pose.orientation.y = 0.0;
    marker2.pose.orientation.z = 0.0;
    marker2.pose.orientation.w = 0.0;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker2.scale.x = 1.0;
    marker2.scale.y = 1.0;
    marker2.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker2.color.r = 0.0f;
    marker2.color.g = 0.0f;
    marker2.color.b = 0.0f;
    marker2.color.a = 1.0;
    marker2.lifetime = ros::Duration();

            // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker3.pose.position.x = 0.75;
    marker3.pose.position.y = 0;
    marker3.pose.position.z = 0.75;
    marker3.pose.orientation.x = 0.5;
    marker3.pose.orientation.y = 0.5;
    marker3.pose.orientation.z = 0.5;
    marker3.pose.orientation.w = 0.5;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker3.scale.x = 0.5;
    marker3.scale.y = 0.5;
    marker3.scale.z = 2.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker3.color.r = 1.0f;
    marker3.color.g = 0.0f;
    marker3.color.b = 0.0f;
    marker3.color.a = 1.0;
    marker3.lifetime = ros::Duration();

        // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker4.pose.position.x = 1.5;
    marker4.pose.position.y = 0;
    marker4.pose.position.z = 0.75;
    marker4.pose.orientation.x = 0.0;
    marker4.pose.orientation.y = 0.0;
    marker4.pose.orientation.z = 0.0;
    marker4.pose.orientation.w = 0.0;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker4.scale.x = 1.0;
    marker4.scale.y = 1.0;
    marker4.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker4.color.r = 1.0f;
    marker4.color.g = 0.0f;
    marker4.color.b = 0.0f;
    marker4.color.a = 1.0;
    marker4.lifetime = ros::Duration();

    // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker5.pose.position.x = 1.5;
    marker5.pose.position.y = 0;
    marker5.pose.position.z = 1.5;
    marker5.pose.orientation.x = 0;
    marker5.pose.orientation.y = 0;
    marker5.pose.orientation.z = 0;
    marker5.pose.orientation.w = 0;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker5.scale.x = 0.5;
    marker5.scale.y = 0.5;
    marker5.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker5.color.r = 1.0f;
    marker5.color.g = 1.0f;
    marker5.color.b = 1.0f;
    marker5.color.a = 1.0;
    marker5.lifetime = ros::Duration();

        // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker6.pose.position.x = 1.5;
    marker6.pose.position.y = 0;
    marker6.pose.position.z = 2.25;
    marker6.pose.orientation.x = 0.0;
    marker6.pose.orientation.y = 0.0;
    marker6.pose.orientation.z = 0.0;
    marker6.pose.orientation.w = 0.0;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker6.scale.x = 1.0;
    marker6.scale.y = 1.0;
    marker6.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker6.color.r = 1.0f;
    marker6.color.g = 1.0f;
    marker6.color.b = 1.0f;
    marker6.color.a = 1.0;
    marker6.lifetime = ros::Duration();

            // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker7.pose.position.x = 0;
    marker7.pose.position.y = -0.75;
    marker7.pose.position.z = 0.75;
    marker7.pose.orientation.x = 0.5;
    marker7.pose.orientation.y = 0.5;
    marker7.pose.orientation.z = 0.5;
    marker7.pose.orientation.w = -0.5;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker7.scale.x = 0.5;
    marker7.scale.y = 0.5;
    marker7.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker7.color.r = 1.0f;
    marker7.color.g = 1.0f;
    marker7.color.b = 1.0f;
    marker7.color.a = 1.0;
    marker7.lifetime = ros::Duration();

        // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker8.pose.position.x = 0;
    marker8.pose.position.y = -1.25;
    marker8.pose.position.z = 0.75;
    marker8.pose.orientation.x = 0.0;
    marker8.pose.orientation.y = 0.0;
    marker8.pose.orientation.z = 0.0;
    marker8.pose.orientation.w = 0.0;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker8.scale.x = 1.0;
    marker8.scale.y = 1.0;
    marker8.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker8.color.r = 1.0f;
    marker8.color.g = 1.0f;
    marker8.color.b = 1.0f;
    marker8.color.a = 1.0;
    marker8.lifetime = ros::Duration();

            // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker9.pose.position.x = -0.75;
    marker9.pose.position.y = 0;
    marker9.pose.position.z = 0.75;
    marker9.pose.orientation.x = 0.5;
    marker9.pose.orientation.y = 0.5;
    marker9.pose.orientation.z = 0.5;
    marker9.pose.orientation.w = 0.5;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker9.scale.x = 0.5;
    marker9.scale.y = 0.5;
    marker9.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker9.color.r = 1.0f;
    marker9.color.g = 1.0f;
    marker9.color.b = 1.0f;
    marker9.color.a = 1.0;
    marker9.lifetime = ros::Duration();

        // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker10.pose.position.x = -1.5;
    marker10.pose.position.y = 0;
    marker10.pose.position.z = 0.75;
    marker10.pose.orientation.x = 0.0;
    marker10.pose.orientation.y = 0.0;
    marker10.pose.orientation.z = 0.0;
    marker10.pose.orientation.w = 0.0;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker10.scale.x = 1.0;
    marker10.scale.y = 1.0;
    marker10.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker10.color.r = 1.0f;
    marker10.color.g = 1.0f;
    marker10.color.b = 1.0f;
    marker10.color.a = 1.0;
    marker10.lifetime = ros::Duration();

        // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker11.pose.position.x = 0;
    marker11.pose.position.y = 0;
    marker11.pose.position.z = -0.75;
    marker11.pose.orientation.x = 0.0;
    marker11.pose.orientation.y = 0.0;
    marker11.pose.orientation.z = 0.0;
    marker11.pose.orientation.w = 0.0;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker11.scale.x = 1.0;
    marker11.scale.y = 1.0;
    marker11.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker11.color.r = 0.0f;
    marker11.color.g = 0.0f;
    marker11.color.b = 0.0f;
    marker11.color.a = 1.0;
    marker11.lifetime = ros::Duration();

            // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker12.pose.position.x = 0;
    marker12.pose.position.y = 0.75;
    marker12.pose.position.z = -0.75;
    marker12.pose.orientation.x = 0.5;
    marker12.pose.orientation.y = 0.5;
    marker12.pose.orientation.z = 0.5;
    marker12.pose.orientation.w = -0.5;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker12.scale.x = 0.5;
    marker12.scale.y = 0.5;
    marker12.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker12.color.r = 1.0f;
    marker12.color.g = 1.0f;
    marker12.color.b = 1.0f;
    marker12.color.a = 1.0;
    marker12.lifetime = ros::Duration();

        // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker13.pose.position.x = 0;
    marker13.pose.position.y = 1.25;
    marker13.pose.position.z = -0.75;
    marker13.pose.orientation.x = 0.0;
    marker13.pose.orientation.y = 0.0;
    marker13.pose.orientation.z = 0.0;
    marker13.pose.orientation.w = 0.0;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker13.scale.x = 1.0;
    marker13.scale.y = 1.0;
    marker13.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker13.color.r = 1.0f;
    marker13.color.g = 1.0f;
    marker13.color.b = 1.0f;
    marker13.color.a = 1.0;
    marker13.lifetime = ros::Duration();

            // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker14.pose.position.x = 0.75;
    marker14.pose.position.y = 0;
    marker14.pose.position.z = -0.75;
    marker14.pose.orientation.x = 0.5;
    marker14.pose.orientation.y = 0.5;
    marker14.pose.orientation.z = 0.5;
    marker14.pose.orientation.w = 0.5;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker14.scale.x = 0.5;
    marker14.scale.y = 0.5;
    marker14.scale.z = 2.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker14.color.r = 1.0f;
    marker14.color.g = 1.0f;
    marker14.color.b = 1.0f;
    marker14.color.a = 1.0;
    marker14.lifetime = ros::Duration();

        // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker15.pose.position.x = 1.5;
    marker15.pose.position.y = 0;
    marker15.pose.position.z = -0.75;
    marker15.pose.orientation.x = 0.0;
    marker15.pose.orientation.y = 0.0;
    marker15.pose.orientation.z = 0.0;
    marker15.pose.orientation.w = 0.0;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker15.scale.x = 1.0;
    marker15.scale.y = 1.0;
    marker15.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker15.color.r = 1.0f;
    marker15.color.g = 1.0f;
    marker15.color.b = 1.0f;
    marker15.color.a = 1.0;
    marker15.lifetime = ros::Duration();

            // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker16.pose.position.x = -0.75;
    marker16.pose.position.y = 0;
    marker16.pose.position.z = -0.75;
    marker16.pose.orientation.x = 0.5;
    marker16.pose.orientation.y = 0.5;
    marker16.pose.orientation.z = 0.5;
    marker16.pose.orientation.w = 0.5;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker16.scale.x = 0.5;
    marker16.scale.y = 0.5;
    marker16.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker16.color.r = 1.0f;
    marker16.color.g = 1.0f;
    marker16.color.b = 1.0f;
    marker16.color.a = 1.0;
    marker16.lifetime = ros::Duration();

        // Set the pose of the marker.  This is a full 6DOF pose relative to the frame/time specified in the header
    marker17.pose.position.x = -1.5;
    marker17.pose.position.y = 0;
    marker17.pose.position.z = -0.75;
    marker17.pose.orientation.x = 0.0;
    marker17.pose.orientation.y = 0.0;
    marker17.pose.orientation.z = 0.0;
    marker17.pose.orientation.w = 0.0;
    // Set the scale of the marker -- 1x1x1 here means 1m on a side
    marker17.scale.x = 1.0;
    marker17.scale.y = 1.0;
    marker17.scale.z = 1.0;
    // Set the color -- be sure to set alpha to something non-zero!
    marker17.color.r = 1.0f;
    marker17.color.g = 1.0f;
    marker17.color.b = 1.0f;
    marker17.color.a = 1.0;
    marker17.lifetime = ros::Duration();


    marker_pub.publish(marker);
    sleep(1);
    marker_pub.publish(marker2);
    sleep(1);
    marker_pub.publish(marker3);
    sleep(1);
    marker_pub.publish(marker4);
    sleep(1);
    marker_pub.publish(marker5);
    sleep(1);
    marker_pub.publish(marker6);
    sleep(1);
    marker_pub.publish(marker7);
    sleep(1);
    marker_pub.publish(marker8);
    sleep(1);
    marker_pub.publish(marker9);
    sleep(1);
    marker_pub.publish(marker10);
    sleep(1);
    marker_pub.publish(marker11);
    sleep(1);
    marker_pub.publish(marker12);
    sleep(1);
    marker_pub.publish(marker13);
    sleep(1);
    marker_pub.publish(marker14);
    sleep(1);
    marker_pub.publish(marker15);
    sleep(1);
    marker_pub.publish(marker16);
    sleep(1);
    marker_pub.publish(marker17);
    sleep(1);
// %EndTag(PUBLISH)%

    // Cycle between different shapes
    //shape = visualization_msgs::Marker::SPHERE;
    //  shape = visualization_msgs::Marker::CYLINDER;

    
// %EndTag(CYCLE_SHAPES)%

// %Tag(SLEEP_END)%
    r.sleep();
  }
// %EndTag(SLEEP_END)%
}
// %EndTag(FULLTEXT)%
